name: viva-env
channels:
  - defaults
dependencies:
  - python = 3.11
  - pip
  
  - pip:
    - sklearn==0.0
    - jupyter-book
    - jupyterlab
    - matplotlib
    - numpy
    - dynasaur
    - objective_rating_metrics
    - reliability
    - ipysheet
    - lifelines
